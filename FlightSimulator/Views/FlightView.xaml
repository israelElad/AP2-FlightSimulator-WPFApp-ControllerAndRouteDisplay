<UserControl flight:Class="FlightSimulator.Views.Flight"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:flight="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FlightSimulator.Views"
             xmlns:views="clr-namespace:FlightSimulator.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <views:FlightBoard Grid.Row="1" Grid.Column="0"/>
        <Viewbox Grid.Row="0" Grid.Column="0">
            <Grid>
                <Button Name="Connect" Content="Connect" Margin="150, 10, 190, 10" Click="Connect_Click" />
                <Button Name="Settings" Content="Settings" Margin="220, 10, 120, 10" Click="Settings_Click"/>
            </Grid>
        </Viewbox>
    </Grid>
</UserControl>
